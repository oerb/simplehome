# Nothing to do here because all is done in simplehome/custom_processors.py and Template with Tag's
# The custom_processors is added by settings.py and returns Global Tags to all Templates over all Apps
